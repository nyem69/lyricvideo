{
  "name": "align-spike",
  "private": true,
  "type": "module",
  "description": "Phase 0 spike: in-browser-class Whisper (transformers.js) -> alignLyrics -> score vs LRC ground truth. Throwaway; gitignored via projects/.",
  "scripts": {
    "transcribe": "tsx transcribe.ts",
    "union": "tsx union.ts",
    "score": "tsx score.ts",
    "estimate": "tsx exp-estimator.ts"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
